# Screenshot URLs – one per line. Optional output path after tab or space.
# Comments and empty lines are ignored.
# Examples:
#   http://localhost:3000
#   http://localhost:3000/groups    groups-list.png
#   http://localhost:3000/groups/xyz/swipe   swipe.png
#
# Ensure the app is running (e.g. npm run dev) for localhost URLs.

http://localhost:3000
http://localhost:3000/groups	groups-list.png
